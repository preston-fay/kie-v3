"""
Setup script for KIE (Kearney Insight Engine).

For development installation:
    pip install -e .

For production installation:
    pip install .
"""

from setuptools import setup

# Configuration is in pyproject.toml
setup()
